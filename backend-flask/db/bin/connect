#!/usr/bin/bash

source $(dirname $0)/../.env

if [ "$ENV_TYPE" = "prod" ]; then
    printf "\033[1;35m == db-connect rds-psql-db \033[0m\n " 
    psql $CONNECTION_URL_DB
else
    printf "\033[1;36m == db-connect localhost-postgresql \033[0m\n " 
    psql $CONNECTION_URL_DB
fi