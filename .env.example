# ENVIRONMENT CONFIGURATION SPLIT INTO TWO FILES
# ===============================================
# This project now uses separate environment files:
# 1. backend-flask/.env - Contains backend/server-side configuration
# 2. frontend-react-js/.env - Contains frontend/client-side configuration



# BACKEND ENVIRONMENT FILE: backend-flask/.env
# ============================================
# Copy this section to backend-flask/.env and update with your values

# Backend configuration
ENVIRONMENT=development
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:5000
FLASK_ENV=development
FLASK_DEBUG=1

# AWS Credentials
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
AWS_REGION=your_aws_region

# AWS Cognito configuration
REACT_APP_AWS_REGION=your_aws_region
REACT_APP_USER_POOL_ID=your_user_pool_id
REACT_APP_USER_POOL_CLIENT_ID=your_client_id

# Honeycomb configuration
HONEYCOMB_API_KEY=your_honeycomb_api_key
OTEL_SERVICE_NAME=backend-flask
OTEL_EXPORTER_OTLP_PROTOCOL="http/protobuf"
OTEL_EXPORTER_OTLP_ENDPOINT=https://api.honeycomb.io
OTEL_EXPORTER_OTLP_HEADERS=x-honeycomb-team=your_honeycomb_api_key

# PostgreSQL database configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
CONNECTION_URL="postgresql://postgres:password@localhost:5432/cruddur"
CONNECTION_URL_DOCKER="postgresql://postgres:password@db:5432/cruddur"
AWS_RDS_PSQL_CONNECTION="postgresql://username:password@your-rds-endpoint:5432/cruddur"
AWS_RDS_PSQL_CONNECTION_DB="postgresql://username:password@your-rds-endpoint:5432"

# Rollbar access token
ROLLBAR_ACCESS_TOKEN=your_rollbar_access_token

# X-Ray daemon configuration
AWS_XRAY_DAEMON_ADDRESS=xray-daemon:2000
AWS_XRAY_TRACING_NAME=cruddur-backend-flask
AWS_XRAY_URL=xray-daemon:2000

# DynamoDB configuration
DYNAMODB_MESSAGES_TABLE="cruddur-messages"
DYNAMODB_CONVERSATIONS_TABLE="cruddur-conversations"
# DynamoDB local for development (comment out for production)
DYNAMODB_LOCAL_DOCKER_URL="http://dynamodb-local:8000"
DYNAMODB_LOCAL_URL="http://localhost:8000"

# FRONTEND ENVIRONMENT FILE: frontend-react-js/.env
# =================================================
# Copy this section to frontend-react-js/.env and update with your values

# Frontend configuration
REACT_APP_BACKEND_URL=http://localhost:5000
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true

# AWS Cognito configuration
REACT_APP_AWS_REGION=your_aws_region
REACT_APP_USER_POOL_ID=your_user_pool_id
REACT_APP_USER_POOL_CLIENT_ID=your_client_id
